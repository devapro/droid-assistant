[Unit]
Description=Nightly droid-assistant backup

[Timer]
OnCalendar=daily
# Survive the machine being off at the scheduled time.
Persistent=true
RandomizedDelaySec=15m

[Install]
WantedBy=timers.target
