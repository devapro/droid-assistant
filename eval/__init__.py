"""The evaluation harness (SRS §4.9).

Standalone: audio files in, WER and DER out. It depends on the backends and on
nothing else in the server, so it can be run against a checkout with no
database, no configuration, and no session.
"""
